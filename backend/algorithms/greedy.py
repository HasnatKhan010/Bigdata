from typing import Any, Dict, List


def run_greedy(origin: Any, candidates: List[Any]) -> Dict[str, Any]:
    # Placeholder for greedy nearest selection.
    nearest = candidates[0] if candidates else None
    return {"choice": nearest, "algorithm": "Greedy"}
