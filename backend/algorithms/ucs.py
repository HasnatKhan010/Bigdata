from typing import Any, Dict, List


def run_ucs(start: Any, goal: Any, graph: Dict[Any, List[Any]]) -> Dict[str, Any]:
    # Placeholder for Uniform Cost Search minimizing cost.
    return {"path": [start, goal], "cost": 0, "algorithm": "UCS"}
